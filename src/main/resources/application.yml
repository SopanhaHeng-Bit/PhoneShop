
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/phoneshop
    username: root
    password: Panha@168
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server:
    port: 2000
